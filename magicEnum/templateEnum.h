#pragma once

/**
 *fare una classe base con gli operatori | & ^ per enum / enum class
 *operator int() che sarebbe to underlyng
 *estendere poi con enum e set
 *in mysql enum inizia da 1, set da 0 (niente)
 *per enum mettere un primo valore di default, spazio bianco è fuorviante, mettere un nome tipo nada ?
 *
 */ 
