#ifndef RANDUTIL_H
#define RANDUTIL_H

#include <QByteArray>
#include <string>

typedef unsigned int uint;

uint rand(uint from, uint to);
//see hash lib
//std::string randString(uint size);

#endif // RANDUTIL_H
